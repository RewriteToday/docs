---
title: "Changelog"
mode: "custom"
description: "Historico oficial de releases da Rewrite, organizado por pacote e pronto para evoluir."
---

<div
  style={{
    position: "relative",
    padding: "88px 20px 56px",
    borderBottom: "1px solid rgba(248, 248, 248, 0.08)",
    background:
      "radial-gradient(1200px 600px at 15% -15%, rgba(255, 255, 255, 0.08), transparent 45%), linear-gradient(180deg, #050505 0%, #070707 55%, #050505 100%)",
  }}
>
  <div style={{ maxWidth: "1100px", margin: "0 auto" }}>
    <div
      style={{
        display: "inline-flex",
        alignItems: "center",
        gap: 8,
        padding: "6px 12px",
        borderRadius: 999,
        border: "1px solid rgba(255,255,255,0.14)",
        background: "rgba(255,255,255,0.03)",
        color: "rgba(255,255,255,0.75)",
        fontSize: 12,
        marginBottom: 14,
      }}
    >
      Release notes oficiais
    </div>

    <h1
      style={{
        fontSize: "42px",
        lineHeight: "1.05",
        fontWeight: 650,
        letterSpacing: "-0.03em",
        margin: "0 0 12px",
        color: "#fff",
      }}
    >
      Changelog da Rewrite
    </h1>

    <p
      style={{
        maxWidth: 760,
        margin: "0",
        fontSize: 18,
        lineHeight: 1.65,
        color: "rgba(255,255,255,0.78)",
        fontWeight: 350,
      }}
    >
      Aqui voce acompanha cada release da Rewrite com contexto claro: o que entrou,
      o que evoluiu e o que exige atencao em API, CLI, SDKs e Webhooks.
    </p>
  </div>
</div>

<div style={{ padding: "44px 20px 8px", maxWidth: "1100px", margin: "0 auto" }}>
  <h2
    style={{
      fontSize: 24,
      fontWeight: 650,
      letterSpacing: "-0.02em",
      margin: "0 0 14px",
      color: "#fff",
    }}
  >
    Ultimas versoes
  </h2>

  <p style={{ margin: "0 0 22px", color: "rgba(255,255,255,0.68)" }}>
    Mantenha sempre as releases mais recentes no topo para facilitar leitura e auditoria.
  </p>

  <Tabs sync={false}>
    <Tab title="Todas">
      <div style={{ marginBottom: 14, color: "rgba(255,255,255,0.72)" }}>Visao consolidada por pacote.</div>
      <ul>
        <li><strong>2026-04-01 API:</strong> Documentado o ajuste do callback do Discord, que agora le os dados de OAuth em query params em vez do body.</li>
        <li><strong>2026-03-31 API:</strong> Adicionadas flags de projeto para suporte a numeros internacionais em rotas selecionadas e documentada a configuracao de `delivery.timeout` e `delivery.retries` em webhooks.</li>
        <li><strong>2026-03-30 API:</strong> Entraram contatos e segmentos, alem do envio por `contact` em mensagens e OTP com propagacao de `contact` e `contactId` nos payloads de webhook.</li>
      </ul>
    </Tab>
    <Tab title="API">
      <ul>
        <li><strong>2026-04-01:</strong> `GET /webhooks/discord` agora espera os valores do callback OAuth em query params. Os exemplos e notas da documentacao foram atualizados para refletir o comportamento real.</li>
        <li><strong>2026-03-31:</strong> Fluxos publicos de envio, OTP e contatos agora documentam a flag de projeto que pode habilitar numeros internacionais alem do perfil padrao `+55`.</li>
        <li><strong>2026-03-31:</strong> Rotas de create/update de webhook agora aceitam `delivery.timeout` e `delivery.retries`, enquanto leituras de webhook expõem `timeout` e `retries` no topo da resposta.</li>
        <li><strong>2026-03-30:</strong> Cobertura completa da referencia para `GET/POST/PATCH/DELETE /contacts` e `GET/POST/PATCH/DELETE /segments`, incluindo attach/detach/list de contatos por segmento.</li>
        <li><strong>2026-03-30:</strong> `POST /messages`, `POST /messages/batch` e `POST /otp` agora aceitam contatos salvos via campo `contact`, alem de numeros diretos em `to`.</li>
        <li><strong>2026-03-30:</strong> A referencia da API agora cobre as rotas faltantes de list/get/create/update para API keys.</li>
      </ul>
    </Tab>
    <Tab title="CLI">
      <ul>
        <li><strong>2026-04-01:</strong> Nenhuma mudanca publica especifica de CLI foi documentada nesta janela.</li>
      </ul>
    </Tab>
    <Tab title="SDKs">
      <ul>
        <li><strong>2026-04-01:</strong> Nenhuma mudanca publica especifica de SDK foi documentada nesta janela.</li>
      </ul>
    </Tab>
    <Tab title="Webhooks">
      <ul>
        <li><strong>2026-03-31:</strong> Recursos de webhook passaram a ter configuracao de retentativas e timeout de entrega.</li>
        <li><strong>2026-03-30:</strong> Payloads de webhook do tipo mensagem agora incluem `contact` e `contactId` quando o envio original partiu de um contato salvo, e os exemplos da doc foram atualizados.</li>
      </ul>
    </Tab>
  </Tabs>
</div>
