---
title: "Retries"
description: "How **Rewrite** retries failed webhooks and how to manually replay events."
---

# Retries & Replays

\- Understand how **Rewrite** guarantees webhook delivery — and what happens when your endpoint fails.

When **Rewrite** sends a webhook event, your server **must respond with `HTTP 200 OK`** to confirm successful delivery.

If your endpoint does not return a `2xx` status code (or times out), the delivery is considered **failed** and **Rewrite** will automatically retry it.

---

## Automatic Retries

Rewrite uses **exponential backoff with capped retries** to ensure reliable delivery without overwhelming your infrastructure.

Each webhook event is retried according to the following schedule:

<div
  className="retry-schedule-table"
  style={{
    borderRadius: '12px',
    overflow: 'hidden',
  }}
>
  <table className="retry-schedule-table__table" style={{ width: '100%',  borderCollapse: 'collapse', border: '1px solid rgba(255, 255, 255, 0.12)' }}>
  <thead>
      <tr>
        <th
          style={{
            textAlign: 'left',
            padding: '12px 14px',
            borderBottom: '1px solid rgba(255, 255, 255, 0.12)',
            borderRight: '1px solid rgba(255, 255, 255, 0.12)',
            background: 'rgba(255, 255, 255, 0.04)',
          }}
        >
          Attempt
        </th>
        <th
          style={{
            textAlign: 'left',
            padding: '12px 14px',
            borderBottom: '1px solid rgba(255, 255, 255, 0.12)',
            background: 'rgba(255, 255, 255, 0.04)',
          }}
        >
          Delay After Previous Failure
        </th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td style={{ padding: '11px 14px', borderBottom: '1px solid rgba(255, 255, 255, 0.08)', borderRight: '1px solid rgba(255, 255, 255, 0.12)' }}>1</td>
        <td style={{ padding: '11px 14px', borderBottom: '1px solid rgba(255, 255, 255, 0.08)' }}>Immediately</td>
      </tr>
      <tr>
        <td style={{ padding: '11px 14px', borderBottom: '1px solid rgba(255, 255, 255, 0.08)', borderRight: '1px solid rgba(255, 255, 255, 0.12)' }}>2</td>
        <td style={{ padding: '11px 14px', borderBottom: '1px solid rgba(255, 255, 255, 0.08)' }}>5 seconds</td>
      </tr>
      <tr>
        <td style={{ padding: '11px 14px', borderBottom: '1px solid rgba(255, 255, 255, 0.08)', borderRight: '1px solid rgba(255, 255, 255, 0.12)' }}>3</td>
        <td style={{ padding: '11px 14px', borderBottom: '1px solid rgba(255, 255, 255, 0.08)' }}>1 minute</td>
      </tr>
      <tr>
        <td style={{ padding: '11px 14px', borderBottom: '1px solid rgba(255, 255, 255, 0.08)', borderRight: '1px solid rgba(255, 255, 255, 0.12)' }}>4</td>
        <td style={{ padding: '11px 14px', borderBottom: '1px solid rgba(255, 255, 255, 0.08)' }}>5 minutes</td>
      </tr>
      <tr>
        <td style={{ padding: '11px 14px', borderRight: '1px solid rgba(255, 255, 255, 0.12)' }}>5</td>
        <td style={{ padding: '11px 14px' }}>15 minutes</td>
      </tr>
    </tbody>
  </table>
</div>

After the final attempt, the webhook is marked as **permanently failed**.

---

### Example

If your endpoint fails the first three attempts but succeeds on the fourth:

1. **Attempt 1** (`t=0s`) - immediate send, **fails**
2. **Attempt 2** (`t=+5s`) - retry after 5 seconds, **fails**
3. **Attempt 3** (`t=+1m 5s`) - retry after 1 minute, **fails**
4. **Attempt 4** (`t=+6m 5s`) - retry after 5 minutes, **success**

The total time from the first attempt to successful delivery would be approximately **6 minutes and 5 seconds**.

---

## When Do Retries Stop?

Retries stop when:

- Your endpoint returns a **2xx response**
- The maximum retry limit is reached
- The webhook endpoint is deleted or disabled
- The event expires (TTL reached)

---

## Best Practices

To avoid unnecessary retries:

- Always return `200 OK` as soon as you receive the event
- Process heavy logic **asynchronously**
- Use idempotency (store event IDs and ignore duplicates)
- Implement request timeout under 10 seconds

Example minimal handler:

```ts
export async function webhookHandler(req, res) {
  const { body } = req;

  // Immediately acknowledge
  res.status(200).end()

  // Process asynchronously
  queue.push(body)
}
```

## Manual Replays

If a webhook fails permanently — or if you need to reprocess an event — you can manually replay it.

You can replay both:

- `failed` deliveries
- `successful` deliveries (useful for debugging)

## How to Replay

1. Go to the **Webhooks** section in the **Rewrite Dashboard**
2. Select your webhook endpoint
3. Open the event you want to replay
4. Click **Replay**

**Rewrite** will resend the exact same payload to your endpoint.

## Idempotency Reminder

Manual replays and automatic retries may deliver the same event multiple times.

You should:

- Store processed event IDs
- Ignore duplicates
- Ensure your system behaves safely on repeated deliveries

## Delivery Guarantees

Rewrite provides:

- At-least-once delivery
- Ordered delivery per message (when applicable)
- Signed payload verification
- Delivery visibility in dashboard

<Card title="Verify webhook signatures" icon="shield-check" href="/webhooks/signatures" color="#C9CBD1"> Always validate the signature header before trusting webhook payloads. </Card>
