---
title: Introducción
description: Aprende a usar el SDK de C# para enviar SMS con la API.
---

## Antes de empezar

- Necesitas .NET 8+ instalado en tu máquina.
- Necesitas una API key de Rewrite con los permisos requeridos.

<Steps>
    <Step title="Instala el SDK" titleSize="h3">
        Para instalar el SDK de C#, ejecuta:

        ```bash
        dotnet add package RewriteToday.SDK
        ```
    </Step>

    <Step title="Configura la variable de entorno REWRITE_API_KEY" titleSize="h3">
    Configura tu API key de Rewrite en las variables de entorno.

    ```text .env
    REWRITE_API_KEY=rw...
    ```
    </Step>

    <Step title="Envía tu SMS" titleSize="h3">
    Ahora solo necesitas pasar el contenido del mensaje y listo.

    ```csharp Program.cs
    using RewriteToday;

    var rewrite = new RewriteClient(Environment.GetEnvironmentVariable("REWRITE_API_KEY"));

    var message = await rewrite.Messages.SendAsync(new SendMessageOptions
    {
      To = "+1234567890",
      Message = "Your OTP code is 478201"
    });

    Console.WriteLine(message);
    ```

    Puedes hacer de todo con nuestro SDK, sigue explorando.
    </Step>
</Steps>

